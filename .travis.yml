dist: trusty
language: python
python:
  - '2.7'
  - '3.3'
  - '3.4'
  - '3.5'

addons:
  apt:
    sources:
      - sourceline: 'ppa:canonical-foundations/ci-train-backports'
    packages:
      - python-lazr.restfulclient

install:
  - pip install -r requirements.txt

script:
  - echo "TEST"