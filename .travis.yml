dist: trusty
language: python
python:
  - '2.7'
  - '3.3'
  - '3.4'
  - '3.5'

before_install:
  - sudo add-apt-repository -y ppa:canonical-foundations/ci-train-backports
  - sudo apt-get -qq update
  - sudo apt-get -y install python-lazr.restfulclient

install:
  - pip install -r requirements.txt

script:
  - echo "TEST"